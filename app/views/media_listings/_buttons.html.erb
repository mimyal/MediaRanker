
<p>
  <%= button_to "Edit", "/#{media_listing.type.downcase}s/#{media_listing.id}/edit", method: :get %>
  <%= button_to "Delete", "/#{media_listing.type.downcase}s/#{media_listing.id}", method: :delete, data: { confirm: "Are you sure that this listing must be deleted?" }, class: "delete-button"%>
  <%= button_to "View all #{media_listing.type.downcase}s", "/#{media_listing.type.downcase}s", method: :get %>
  <%= button_to "View all media", root_path, method: :get %>
</p>
<p>This is the buttons partial view from the show form</p>
